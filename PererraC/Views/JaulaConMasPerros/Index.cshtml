@model IEnumerable<PererraC.Models.ViewModels.JaulaConMasPerros>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.jaulaNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numeroPerros)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombresPerros)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.jaulaNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numeroPerros)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombresPerros)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
